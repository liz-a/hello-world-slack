<%= form_with(model: guess, local: true) do |form| %>
  <% if guess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guess.errors.count, "error") %> prohibited this guess from being saved:</h2>

      <ul>
      <% guess.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :guess %>
    <%= form.text_field :guess %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :correct_or_incorrect %>
    <%= form.number_field :correct_or_incorrect %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
